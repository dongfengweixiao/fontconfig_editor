import 'package:flutter/material.dart';

class FontConfigOptionPage extends StatelessWidget {
  const FontConfigOptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Font config options page.'),
    );
  }
}
