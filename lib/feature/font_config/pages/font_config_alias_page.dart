import 'package:flutter/material.dart';

class FontConfigAliasPage extends StatelessWidget {
  const FontConfigAliasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Font config alias page.'),
    );
  }
}
